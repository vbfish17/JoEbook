# Optional shared Gemini API key for server-side requests.
# If omitted, users can still provide their own provider credentials in the app UI.
GEMINI_API_KEY="your_gemini_api_key"

# Optional application URL used by deployed environments.
APP_URL="http://localhost:3000"
